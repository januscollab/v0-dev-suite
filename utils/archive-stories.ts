// Utility function for archiving stories
export function archiveStories(storyIds: string[]): number {
  // This is a placeholder implementation
  // In the actual app, this would move stories to archived sprints
  console.log("Archiving stories:", storyIds)
  return storyIds.length
}
